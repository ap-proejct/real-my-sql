# 7. 데이터 암호화 (Data Encryption)

## 📌 버전별 암호화 지원 범위

| 버전 | 지원 범위 |
|---|---|
| **MySQL 5.7** | 데이터 파일(테이블스페이스)만 암호화 지원 |
| **MySQL 8.0** | 데이터 파일, 리두 로그, 언두 로그, 바이너리 로그 등 전체 암호화 지원 |

---

## 7-1. MySQL 서버의 데이터 암호화

### 동작 원리

**암호화 지점**
> 데이터베이스 서버와 디스크 사이의 데이터 읽기/쓰기 지점에서 암/복호화 수행
> 
> **InnoDB 스토리지 엔진의 I/O 레이어에서만 실행**

### TDE (Transparent Data Encryption)

**투명한 데이터 암호화**
- MySQL 내부 및 사용자 입장에서 암호화 여부에 상관없이 동일하게 동작
- 애플리케이션 코드 변경 없이 암호화 적용 가능

---

## 7-1-1. 2단계 키 관리

### 키링 플러그인 (Keyring Plugin)

MySQL 서버의 TDE 암호화 키는 키링 플러그인으로 관리됩니다.

**사용 가능한 키링 플러그인**

| 플러그인 | 설명 | 에디션 |
|---|---|---|
| **keyring_file** | File-Based 플러그인 | 커뮤니티 에디션 가능 |
| **keyring_encrypted_file** | Keyring 플러그인 | 엔터프라이즈 |
| **keyring_okv** | KMIP 플러그인 | 엔터프라이즈 |
| **keyring_aws** | Amazon Web Services Keyring 플러그인 | 엔터프라이즈 |

---

### 2단계 암호화 키 구조

MySQL 서버 데이터 암호화는 **마스터 키**와 **테이블스페이스 키** 2가지 종류의 키로 이루어집니다.

#### 🔑 테이블스페이스 키 (Tablespace Key)

**특징**
- 암호화된 테이블이 생성될 때마다 해당 테이블을 위한 임의의 키 발급
- 마스터 키를 이용해 암호화되어 각 테이블의 데이터 파일 헤더에 저장
- MySQL 서버 외부로 노출되지 않음
- 주기적 변경 불필요 (보안상 취약점 발생 X)

#### 🔐 마스터 키 (Master Key)

**특징**
- 외부 파일을 이용하기 때문에 노출 가능성 존재
- 주기적인 변경 권장

**마스터 키 변경 명령어**
```sql
ALTER INSTANCE ROTATE INNODB MASTER KEY;
```

**마스터 키 변경 프로세스**
```
1. 새로운 마스터 키 생성
2. 각 테이블스페이스 키를 기존 마스터 키로 복호화
3. 새로운 마스터 키로 테이블스페이스 키를 다시 암호화
4. 데이터 파일의 헤더 부분만 업데이트
```

> 💡 **2단계 암호화 방식을 채택한 이유**
> 
> 테이블스페이스 키가 직접 변경된다면, MySQL 서버는 데이터 파일의 **모든 데이터를 다시 암/복호화**해야 합니다. 이는 엄청난 부하와 시간이 소요되므로, 2단계 방식으로 마스터 키만 변경하여 효율성을 높입니다.

---

### TDE 지원 암호화 알고리즘

**AES 256 알고리즘 사용**

| 대상 | 암호화 알고리즘 |
|---|---|
| **테이블스페이스 키** | AES-256 ECB |
| **데이터 파일** | AES-256 CBC |

---

## 7-1-2. 암호화와 성능

### 성능 영향 분석

#### 읽기 작업 (Read)

**복호화 과정**
```
1. 쿼리가 InnoDB 버퍼 풀에 적재되지 않은 데이터를 읽음
2. 디스크에서 데이터 읽기
3. 복호화 과정 수행 ← 지연 발생
4. 쿼리 결과 반환
```

> ⚠️ 암호화된 테이블이 **3~5배 정도 느림**

#### 쓰기 작업 (Write)

**암호화 과정**
```
1. 암호화된 테이블 변경
2. 디스크로 동기화 시 암호화 수행 ← 지연 발생
3. 디스크에 저장
```

> ⚠️ 암호화된 테이블이 **5~6배 정도 느림**

**하지만!**
- 데이터 페이지 저장은 **백그라운드 스레드**가 수행
- 실제 사용자 쿼리가 직접적으로 지연되는 것은 아님

---

### 메모리 효율성

**버퍼 풀에 미치는 영향**
- 데이터 페이지가 암호화 키보다 훨씬 크기 때문에 암호화 결과가 평문과 동일한 크기
- 암호화한다고 해서 InnoDB 버퍼 풀의 효율이 달라지거나 메모리 사용 효율이 떨어지지 않음

---

### 압축과 암호화의 순서

**압축 → 암호화 순서로 실행**

#### 압축이 먼저 실행되는 이유

1. **압축률 보존**
   - 암호화된 결과문은 아주 랜덤한 바이트 배열을 가짐
   - 이는 압축률을 상당히 떨어뜨림

2. **버퍼 풀 효율성**
   - 압축된 데이터 페이지는 버퍼 풀에 압축 또는 압축 해제의 모든 상태로 존재
   - 암호화는 버퍼 풀 외부(디스크 I/O)에서만 수행

**처리 순서**
```
데이터 → 압축 → 암호화 → 디스크 저장
디스크 읽기 → 복호화 → 압축 해제 → 버퍼 풀
```

---

## 7-1-3. 암호화와 복제

### 레플리카 서버의 암호화

**키 관리**
- 소스 서버와 레플리카 서버는 **서로 다른 마스터 키**와 **테이블스페이스 키**를 가짐
- 암호화 이전 평문 데이터는 동일
- 실제 암호화된 데이터 파일 내용은 전혀 다름

**장점**
- 각 서버가 독립적인 암호화 키를 사용
- 한 서버의 키가 노출되어도 다른 서버는 안전

---

## 7-2. 테이블 암호화

테이블 암호화는 **어떤 키링 플러그인을 사용하더라도 동일하게 동작**합니다.

### 7-2-1. 테이블 생성

#### 암호화된 테이블 생성
```sql
-- 암호화 테이블 생성
CREATE TABLE encrypted_table (
    id INT PRIMARY KEY,
    name VARCHAR(100),
    email VARCHAR(100)
) ENCRYPTION='Y';
```

#### 기존 테이블 암호화 적용
```sql
-- 기존 테이블에 암호화 적용
ALTER TABLE existing_table ENCRYPTION='Y';
```

---

### 암호화된 테이블 확인
```sql
-- 암호화된 테이블 검색
SELECT 
    table_schema, 
    table_name, 
    create_options 
FROM information_schema.tables 
WHERE table_name = 'encrypted_table';
```

**결과 예시**
```
+---------------+------------------+-----------------+
| table_schema  | table_name       | create_options  |
+---------------+------------------+-----------------+
| mydb          | encrypted_table  | ENCRYPTION='Y'  |
+---------------+------------------+-----------------+
```

---

### 7-2-2. 테이블스페이스 이동 (Export & Import)

#### 일반 테이블 이동

테이블스페이스 이동 기능은 레코드를 덤프하고 복구하는 방식보다 **훨씬 효율적이고 빠릅니다**.

#### 암호화된 테이블 이동

> ⚠️ **주의사항**
> 
> TDE가 적용된 암호화 테이블의 경우, 원본 서버와 목적지 서버의 암호화 마스터 키가 다르기 때문에 추가 작업이 필요합니다.

**익스포트 과정**
```sql
-- 1. 원본 서버에서 테이블 익스포트
FLUSH TABLES source_table FOR EXPORT;
```

**생성되는 파일**
- `source_table.ibd` - 데이터 파일
- `source_table.cfg` - 메타데이터 파일
- `source_table.cfp` - 임시 마스터 키 파일 ⭐

> 💡 **중요**
> 
> 암호화된 테이블의 테이블스페이스 이동 시 **반드시 데이터 파일과 .cfp 파일을 같이 복사**해서 전달해야 합니다.

**복사 예시**
```bash
# 원본 서버에서
cp /var/lib/mysql/mydb/source_table.ibd /backup/
cp /var/lib/mysql/mydb/source_table.cfg /backup/
cp /var/lib/mysql/mydb/source_table.cfp /backup/  # 암호화 테이블만 필요

# 목적지 서버로 파일 전송
scp /backup/source_table.* user@destination:/restore/
```

---

## 7-3. 언두 로그 및 리두 로그 암호화

### 기존 방식의 문제점

**데이터 파일만 암호화**
- 테이블의 암호화는 테이블의 데이터를 디스크에 읽기/쓰기할 때만 암/복호화 진행
- 데이터 파일 이외의 파일(리두, 언두, 바이너리 로그)는 **평문으로 디스크에 저장** ⚠️

---

### MySQL 8.0.16 버전 이후 개선

**새로운 시스템 변수 추가**
```sql
-- 언두 로그 암호화 활성화
SET GLOBAL innodb_undo_log_encrypt = ON;

-- 리두 로그 암호화 활성화
SET GLOBAL innodb_redo_log_encrypt = ON;
```

**암호화 방식**
- 리두 로그와 언두 로그 데이터 모두 **각각의 테이블스페이스 키로 암호화**
- 다른 테이블스페이스 키와 마찬가지로 새로운 마스터 키 발급 시 다시 암호화

---

## 7-4. 바이너리 로그 암호화

### 개요

바이너리 로그와 릴레이 로그 파일 암호화 기능은 **디스크에 저장된 로그 파일에 대한 암호화만 담당**합니다.

---

### 7-4-1. 바이너리 로그 암호화 키 관리

바이너리 로그와 릴레이 로그 또한 **2단계 암호화 키 관리 방식**을 사용합니다.

**키 구조**

| 테이블 암호화 | 바이너리 로그 암호화 |
|---|---|
| 마스터 키 | 바이너리 로그 암호화 키 |
| 테이블스페이스 키 | 바이너리 로그 파일 키 |

---

### 7-4-2. 바이너리 로그 암호화 키 변경

#### 변경 명령어
```sql
ALTER INSTANCE ROTATE BINLOG MASTER KEY;
```

#### 변경 프로세스
```
1. 증가된 시퀀스 번호와 함께 새로운 바이너리 로그 암호화 키 발급
   → 키링 파일에 저장

2. 바이너리 로그 파일과 릴레이 로그 파일 스위치
   → 새로운 로그 파일로 로테이션

3. 새로 생성되는 로그 파일의 파일 키 생성
   → 바이너리 로그 파일 키로 암호화하여 각 로그 파일에 저장

4. 기존 로그 파일의 파일 키를 읽어서
   → 새로운 바이너리 로그 파일 키로 암호화하여 다시 저장
   (암호화되지 않은 로그 파일은 무시)

5. 모든 로그 파일이 새로운 키로 암호화 완료되면
   → 기존 바이너리 로그 암호화 키를 키링 파일에서 제거
```

---

#### 암호화 확인
```sql
-- 바이너리 로그 암호화 상태 확인
SHOW BINARY LOGS;
```

**결과 예시**
```
+-------------------+-----------+-----------+
| Log_name          | File_size | Encrypted |
+-------------------+-----------+-----------+
| mysql-bin.000001  | 177       | Yes       |
| mysql-bin.000002  | 177       | Yes       |
+-------------------+-----------+-----------+
```

---

### 7-4-3. mysqlbinlog 도구 활용

#### 문제 상황

**암호화된 바이너리 로그 접근 제약**
- 암호화된 바이너리 로그를 가져오더라도 바이너리 로그 암호화 키가 없으면 복호화 불가
- 키는 MySQL 서버에서만 관리되므로 외부에서 직접 복호화 불가능

#### 해결 방법

**MySQL 서버에서 직접 복호화하여 가져오기**
```bash
# MySQL 서버에 접속하여 바이너리 로그 읽기
mysqlbinlog \
  --read-from-remote-server \
  -uroot \
  -p \
  -vvv \
  mysql-bin.000001
```
---